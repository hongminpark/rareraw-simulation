import React from "react";
import ReactDOM from "react-dom/client";
import VitsoeApp from "./VitsoeApp.tsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
    <React.StrictMode>
        <VitsoeApp />
    </React.StrictMode>
);
